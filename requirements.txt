# Full-Stack Development with Python - Dependencies

# Web Frameworks (choose one for each project)
Flask==3.0.0
Flask-Login==0.6.3  # For authentication (Project 4)
Werkzeug==3.0.3  # Patched version - fixes remote execution vulnerability

# Alternative framework
fastapi==0.109.1  # Patched version - fixes ReDoS vulnerability
uvicorn[standard]==0.24.0  # ASGI server for FastAPI
python-multipart==0.0.18  # Patched version - fixes DoS and ReDoS vulnerabilities
Jinja2==3.1.2  # Templates for FastAPI

# Optional but recommended
python-dotenv==1.0.0  # Environment variables
